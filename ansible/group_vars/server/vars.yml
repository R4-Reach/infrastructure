---
webapp_database_password: '{{ vault_webapp_database_password }}'

# Postgres superuser password; only used by the database container itself
# (set at initdb). Ansible connects as postgres over the container-local
# unix socket, which needs no password.
webapp_database_superuser_password: '{{ vault_webapp_database_superuser_password }}'
