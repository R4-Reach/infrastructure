# Managed by Ansible (sshd role) — do not edit by hand.
#
# Disallow SSH login as root entirely. Root is reachable only through the
# Linode Lish/Glish console (the admin_user role sets the root password for
# exactly that purpose). Admins log in as their own sudo-enabled user over SSH.
PermitRootLogin no
